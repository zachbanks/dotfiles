# ==========
#= Custom =
# ==========

editor="mvim"
# Make Git (and possibly other programs) use mvim as default editor.
export EDITOR="/usr/bin/${editor}"

# Add bin directory in home to path.
export PATH=$PATH:/users/zachbanks/bin/